//
// For the copyright information for this file, please search up the
// directory tree for the first COPYING file.
//

[[cl_sst_is_little_endian,sst::is_little_endian]]
= The `sst::is_little_endian` trait

.{cpp}
[source,cpp,subs="normal"]
----
#include <link:{repo_browser_url}/src/c_cpp/include/sst/catalog/is_little_endian.hpp[sst/catalog/is_little_endian.hpp]>
namespace sst {

template<class T>
struct is_little_endian
  : {ce_std_integral_constant}<bool, /*...*/> {};

}
----

The `sst::is_little_endian` trait determines whether the byte ordering
of `T` is little endian.

Any {cr_cv_qualifiers} on `T` are ignored.

The `sst::is_little_endian` trait may produce false negatives.

:W: sst::is_little_endian
:T: T
:V: bool
include::cl_integral_constant_extraction_blurb.adoc[]

//
